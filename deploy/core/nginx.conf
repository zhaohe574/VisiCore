map $scheme $visicore_forwarded_proto {
    default $scheme;
}

server {
    listen 8080;
    server_name _;
    include /etc/nginx/snippets/visicore-server.conf;
}

include /run/nginx/visicore-https.conf;
