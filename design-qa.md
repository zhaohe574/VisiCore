# 设计 QA

## 对比目标

- 目标：首页只保留项目首屏与 Windows 桌面端下载，管理端入口保持低强调。
- 实现：`web-admin/src/App.vue`、`web-admin/src/styles.css`、`web-admin/src/api.ts`、`web-admin/vite.config.ts`。
- 预览：`http://localhost:4174/`，桌面端 1440×1024，移动端 390×844。

## 已检查

- 此为更名前的历史首屏验收记录；当前品牌统一为 VisiCore（视枢），更名范围见 `docs/VisiCore-更名说明.md`。
- 首屏使用真实安防摄像机图片，按钮、锚点导航、下载状态和管理入口均可交互。
- `#admin` 可直达管理端登录，登录页可返回公开首页；登录 401 会停留在登录页并提示账号或密码不正确，管理台原有导航和权限逻辑保留。
- 390px 移动视口无横向溢出；桌面首屏标题、图片和下载面板无裁切或重叠。
- 浏览器控制台无 error 或 warning；`npm run build` 和 API `dotnet build` 均通过。

## 已知边界

- 没有提供独立视觉参考稿，本次按需求 brief 和现有管理台令牌进行设计 QA。
- 公开下载区依赖服务端存在已发布版本；无版本时展示联系管理员的空状态。
- 本地 Vite 预览默认代理到部署 API；可用 `VITE_API_PROXY_TARGET` 覆盖为本地 API。

## 2026-09-06 管理端复查

- 使用本机模拟 API，对 10 个管理页面分别执行 1440×1024 与 390×844 视口检查，均无页面横向溢出。
- 修复统计圆环和数值区域挤压、移动报警固定操作列遮挡、16 分屏空状态文字重叠。报警事件类型筛选已通过界面操作确认结果变化。
- 移除公开首页硬编码的“现场链路稳定”和同步时间，避免将静态装饰当作设备实际状态。
- 本轮模拟 API 不提供 SignalR 和实际媒体，连接错误是测试环境预期；真实登录、播放、截图、PTZ 与权限隔离需联调环境确认。
- Windows WPF 本地检查覆盖分屏、搜索、播放器释放和窗口关闭；桌面控制工具未通过窗口归属校验，未声称完成桌面截图验收。
