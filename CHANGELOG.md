# 变更记录

## 桌面 2.1.4：25路分屏与权限控制、消除黑屏空位（1+7 / 1+9）及系统设置联动

- **分屏布局优化与黑屏消除**：
  - 将原 1+5 与 1+7 布局重构升级为 1+7 与 1+9，大画面聚焦结合周边小窗格紧密环绕，彻底消除右下角黑屏空位问题。
  - 1+7 采用 4×4 网格（左上 3×3 大屏 + 7 路小屏），1+9 采用 5×5 网格（左上 4×4 大屏 + 9 路小屏），网格 100% 满铺。
- **新增 25 路分屏与细粒度权限控制**：
  - 桌面端分屏扩展至 25 路（5×5 网格），Tiles 播放器容量扩充至 25 路。
  - 引入 `live.split25` 权限，支持后台按用户或角色细粒度分配 25 路分屏能力；未授权用户在分屏栏中智能隐藏 25 路按钮，拦截未经授权的切换并在权限失效时平滑降级。
  - 数据库新增 `010_layout25_and_permissions.sql` 迁移脚本，放开 25 路布局约束并将系统设置每用户预览路数默认放宽对齐。
- **系统设置与媒体流配额无冲突动态联动**：
  - 系统设置默认每用户实时预览路数（`LivePerUser`）提升为 25。
  - 服务端媒体取流配额检查与 `live.split25` 权限联动，拥有 25 路分屏权限的用户上限动态保障至少 25 路，彻底避免前后台限制冲突导致的 429 配额错误。

## 桌面 2.1.3：回退实验性节能解码参数，消除丢帧卡顿

- **回退实验性节能解码参数与配置项**：
  - 彻底移除实验性的 `--drop-late-frames`、`--skip-frames`、`--avcodec-skiploopfilter=4` 与 `--avcodec-fast` 解码参数，消除因强行丢帧和跳过关键参考帧运算导致的视频画面顿挫、掉帧与卡顿。
  - 恢复标准的稳定硬件与软件解码流水线，保持流畅、平稳的实时视频播放效果。
  - 清理“设置 → 视频解码与呈现”中的实验性节能模式开关，保持设置界面简洁稳定。

## 桌面 2.1.2：分屏轻量节能解码模式（低配终端性能优化）

- **分屏轻量节能解码模式（低配终端性能优化）**：
  - 针对分屏小窗格在 LibVLC 播放器与媒体流中注入 `--avcodec-skiploopfilter=4` 与 `:avcodec-skiploopfilter=4`，彻底跳过高算力的去块滤波（Deblocking Loop Filter）平滑运算，为老旧/低配 CPU 软解节省 20%~30% 计算周期，小窗格肉眼无损。
  - 注入 `--avcodec-fast` 快速加速解码通道，并开启 `--drop-late-frames` 与 `--skip-frames` 机制，防止网络偶发抖动或系统高负载时渲染帧滞后堆积。
  - 在“设置 → 图像与解码”中增加“多分屏开启轻量节能解码模式（低配机推荐）”开关，默认开启，支持运行时热切换重置原生播放引擎，配置本地自动持久化。

## 桌面 2.1.1：分屏悬浮菜单显示修复与全局鼠标钩子体验优化

- **分屏浮动操作菜单显示与布局修复**：
  - 彻底修复 `TileHeaderPopup` 跨 WPF NameScope 作用域无法继承 DataContext 的根本缺陷，确保通道标题、码流类型、窗口号与控制按钮正常呈现。
  - 重构顶部浮动栏布局为受约束的 `Grid` 双列结构，右侧操作按钮与角标（静音、一键抓图、单窗放大/还原、停止播放）优先完整计量展示，杜绝按钮被挤出屏幕。
  - 左侧通道标题提炼为 `HeaderTitle`，去除多余冗余的“ · 预览”，并启用 `TextTrimming="CharacterEllipsis"`，确保长名称自适应省略且永远不挤占右侧操作按钮。
  - 单窗放大/还原按钮依据实际状态动态联动，平滑切换提示与图标。
  - 支持在顶部浮动栏任意区域右键点击直接呼出分屏上下文菜单。
- **窗口缩小/还原/最小化时悬浮操作菜单异常弹出问题根治**：
  - 全局低级鼠标移动钩子加入 Win32 `WindowFromPoint` 与本进程 PID 校验，鼠标移出主程序窗口（进入桌面、任务栏或其它应用）即刻清空隐藏所有分屏操作菜单。
  - 严格校验主窗口最小化与主窗口可视客户区边界，窗口最小化或鼠标在窗口范围外时拒绝响应并执行清退。
  - 严格过滤非活跃格位（`IsActive && IsPlaying`），彻底排除单窗放大时隐藏的其余 15 路格位或未播放格位。
  - 实现全局互斥机制：全局至多仅允许当前悬停的单个分屏窗格呈现操作菜单，鼠标移入新格位即刻关闭其它格位菜单，杜绝多路菜单同时常驻。
  - 主窗口生命周期深度联动：在窗口大小变动（`OnRenderSizeChanged`）、位置拖动（`OnLocationChanged`）、状态切换（`OnStateChanged`，最小化/还原）以及失焦（`Deactivated`）时统一主动清空浮动菜单。
  - 分屏模式切换（1/4/9/16）与单窗放大/还原（`ToggleMaximize`）时同步清退菜单，杜绝遗留悬浮条。

## 桌面 2.1.0：iVMS-4200 布局/配色重构与性能优化

- **配色体系重构**：新增 `Themes/Light.xaml`、`Themes/Dark.xaml` 与 `Themes/Controls.xaml`，把原先散落在各视图的内联颜色全部收敛为主题令牌。默认改为 iVMS-4200 主界面观感的**浅色主题**（浅灰页面 + 白色面板 + 深色顶栏/导航 + 蓝色强调），设置页可一键切换深色；控件模板统一改用 `DynamicResource`，换肤不需要重建界面。
- **浅色主题可读性修复**：加深次要文字、边框、状态色与反色文字，WCAG AA 对比度门禁全部通过。
- **去除重复导航**：删除左侧冗余功能竖栏，导航统一为顶栏选项卡，横向视界完整让渡给分屏视频墙。
- **分屏档位扩展**：新增 `VideoWallPanel` 与 `LayoutPreset`，全面支持 1 / 4 / 9 / 16 与聚焦 1+5、1+7 档位。
- **CPU 与多路并发优化**：分屏优先子码流、不可见格位释放、批量加载闸门（错峰建流）、移除时钟同步抑制。

## 桌面 2.0.5：VisiCore（视枢）

- 彻底修复多路并发拉流超时问题，引入通道细粒度锁与错峰阶梯调度。

## 桌面 2.0.4：VisiCore（视枢）

- 优化多路视频预览加载机制：支持最多 16 路分屏视频完全并行并发加载（并发创建媒体会话与并发拉流解码），彻底告别以往逐路排队串行加载，全屏多分屏与视图切换秒级齐开。
- 支持双击左侧组织分组节点批量并行加载该分组下所有在线通道（最多 16 路）。
- 强化播放器工厂与高并发创建视频实例的线程安全性。
- Web 端首页更新预览图为 VisiCore（视枢）标准工作区实况界面，移除历史旧品牌元素与免责说明，并新增版本更新日志（Changelog）时间轴模块。
- 后台版本发布全面改造为一个版本同时联合上传 MSI 安装包与 ZIP 便携包，支持版本号严格校验与查重，未发布草稿版本支持修改与安全删除（自动清理磁盘包文件）。
- 完整包含 2.0.3 继承的所有功能（退出即刻响应零卡顿、真全屏覆盖任务栏、分屏自适应铺满无黑边及原始比例选项）。

## 桌面 2.0.3：VisiCore（视枢）

- 修复关闭客户端时主界面停留冻结、等待数秒才退出的问题：点击关闭立即脱离任务栏并隐藏主窗体与底层视频图层，后台异步释放媒体会话与播放器硬解资源，并加入有界超时保护，退出即刻响应零卡顿。
- 完整包含 2.0.2 版本所有功能：仿海康威视 iVMS-4200 交互架构、独占真全屏覆盖任务栏、分屏实时自适应铺满无缝拉伸及原始比例留黑边选项。

## 桌面 2.0.2：VisiCore（视枢）

- 项目更名，更新 Web、桌面、更新器、安装向导和 OpenAPI 的产品名称。
- 新增统一品牌标识，解决方案入口整理为 `VisiCore.slnx`，更新源码说明与忽略规则。
- 纳入第二版全端重构源码、数据库迁移、生成客户端及自动验证工具。
- C# 生成客户端随源码提交，默认构建不再依赖被忽略的本机产物目录。
- 修复值守工具在账号撤销或密码重置后持续重登的问题，认证拒绝即停止本轮。
- 保留现有命名空间、部署路径、MSI 升级标识及本机加密配置，详见更名说明。
- 将 Microsoft.OpenApi 固定到 2.7.5，修复 GHSA-v5pm-xwqc-g5wc 的循环引用解析漏洞。
- 桌面端界面进一步仿照海康威视 iVMS-4200 布局与使用逻辑：主预览视图标签栏（点击切换已保存视图、新建／保存视图、轮巡开关）、远程回放左侧日历检索面板与精简底部时间轴、报警中心实况／录像一键联动、顶部用户下拉菜单、窗口编号角标与状态栏时钟。
- 修复全屏显示时 Windows 桌面底部任务栏残留遮挡画面的问题：全屏模式下动态脱离 WindowChrome 约束并启用真全屏独占置顶，退出全屏恢复工作区布局。
- 修复分屏“自适应铺满”未能彻底铺满分屏（两侧残留黑边）的问题：基于分屏容器实际尺寸动态计算渲染比例驱动原生播放器自适应拉伸，彻底杜绝黑边；右键菜单新增“原始比例”选项。

## 已有桌面 2.0.1 基线

- 修复分屏溢出、分屏切换和窗口放大还原时的黑屏。
- 增加 RTSP 不可达时的 HTTPS 媒体回退和有界重试。
- MSI／ZIP 双包发布与完整性校验已有记录；本节不代表 VisiCore 品牌包已发布。

## 平台 2.0.0 基线

- .NET 10 模块化服务、Vue 管理端、WPF 值守端及多设备适配。
- 第二版认证权限、媒体、报警、导出、审计、持久化任务与版本管理。

实际通过范围和剩余限制以 `docs/v2-验收清单.md` 为准。
