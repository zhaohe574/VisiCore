<UserControl x:Class="VideoPlatform.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{DynamicResource TextBrush}">
    <Grid Background="{DynamicResource PageBrush}">
        <Border Style="{StaticResource PanelCard}" Width="400" Padding="28,26" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <Image Source="/Assets/Brand.png" Width="40" Height="40" Margin="0,0,12,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="VisiCore · 视枢" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource TextStrongBrush}"/>
                        <TextBlock Text="工业视频综合管理系统 · 值守端" FontSize="11" Foreground="{DynamicResource MutedBrush}" Margin="0,3,0,0"/>
                    </StackPanel>
                </StackPanel>
                <Border Style="{StaticResource HeaderDivider}" Margin="0,16,0,16"/>

                <TextBlock Text="平台地址" FontSize="11" Foreground="{DynamicResource MutedBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding Server, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="平台地址"/>
                <TextBlock Text="账号" FontSize="11" Foreground="{DynamicResource MutedBrush}" Margin="0,14,0,6"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="账号"/>
                <TextBlock Text="密码" FontSize="11" Foreground="{DynamicResource MutedBrush}" Margin="0,14,0,6"/>
                <PasswordBox x:Name="Password" KeyDown="PasswordKeyDown" AutomationProperties.Name="密码"/>

                <Button Content="登 录" Click="LoginClicked" Margin="0,22,0,0" Height="36">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <TextBlock Text="{Binding Status}" TextWrapping="Wrap" Margin="0,14,0,0" Foreground="{DynamicResource AccentTextBrush}" FontSize="11"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

