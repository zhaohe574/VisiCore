<UserControl x:Class="VideoPlatform.Desktop.Views.ExportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{DynamicResource TextBrush}">
    <Grid Background="{DynamicResource PageBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 面板标题栏 -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,0,0,1" Padding="16,10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="&#xE72C;" Style="{StaticResource IconButton}" ToolTip="刷新任务列表" Command="{Binding RefreshJobsCommand}" Margin="0,0,4,0"/>
                    <Button Content="&#xE896;" Style="{StaticResource IconButton}" ToolTip="下载已完成录像" Command="{Binding DownloadCommand}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="录像导出" Style="{StaticResource PanelHeaderText}"/>
                    <Border Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" CornerRadius="3" Padding="7,1" Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Total, StringFormat='共 {0} 个任务'}" FontSize="11" Foreground="{DynamicResource MutedBrush}"/>
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- 任务操作行 -->
        <Border Grid.Row="1" Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,0,0,1" Padding="16,8">
            <StackPanel Orientation="Horizontal">
                <Button Content="取消任务" Command="{Binding CancelJobCommand}" Margin="0,0,8,0"/>
                <Button Content="重试任务" Command="{Binding RetryCommand}" Margin="0,0,8,0"/>
                <Button Style="{StaticResource PrimaryButton}" Command="{Binding DownloadCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE896;" FontFamily="Segoe MDL2 Assets" FontSize="11" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="下载到本地" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}" Margin="16,12,16,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="通道" Binding="{Binding ChannelName}" Width="*"/>
                <DataGridTextColumn Header="开始时间" Binding="{Binding Start, StringFormat={}{0:MM-dd HH:mm:ss}}" Width="145"/>
                <DataGridTextColumn Header="结束时间" Binding="{Binding End, StringFormat={}{0:MM-dd HH:mm:ss}}" Width="145"/>
                <DataGridTextColumn Header="状态" Binding="{Binding StateLabel}" Width="85"/>
                <DataGridTextColumn Header="进度" Binding="{Binding Progress, StringFormat={}{0:0}%}" Width="70"/>
                <DataGridTextColumn Header="文件" Binding="{Binding FileName}" Width="*"/>
                <DataGridTextColumn Header="到期时间" Binding="{Binding ExpiresAt, StringFormat={}{0:MM-dd HH:mm}}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 错误与下载进度 -->
        <StackPanel Grid.Row="3" Margin="16,0,16,8">
            <TextBlock Text="{Binding Selected.Error}" Foreground="{DynamicResource AccentTextBrush}" TextWrapping="Wrap" FontSize="11"/>
            <DockPanel Visibility="{Binding IsDownloading, Converter={StaticResource BoolVisibility}}" Margin="0,6,0,0">
                <Button Content="取消下载" DockPanel.Dock="Right" Command="{Binding CancelDownloadCommand}" Margin="8,0,0,0"/>
                <ProgressBar Minimum="0" Maximum="100" Value="{Binding DownloadProgress}" Height="12" VerticalAlignment="Center"/>
            </DockPanel>
        </StackPanel>

        <!-- 分页与状态 -->
        <Border Grid.Row="4" Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,1,0,0" Padding="16,6">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBlock Text="{Binding PageNumber, StringFormat='第 {0} 页'}" Margin="8,0" FontSize="11" Foreground="{DynamicResource MutedBrush}"/>
                    <Button Content="&#xE76B;" Style="{StaticResource IconButton}" ToolTip="上一页" Command="{Binding PreviousPageCommand}"/>
                    <Button Content="&#xE76C;" Style="{StaticResource IconButton}" ToolTip="下一页" Command="{Binding NextPageCommand}"/>
                </StackPanel>
                <TextBlock Text="{Binding Status}" TextTrimming="CharacterEllipsis" FontSize="11" Foreground="{DynamicResource MutedBrush}" ToolTip="{Binding Status}"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>

