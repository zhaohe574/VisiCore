# VisiCore（视枢）全端 2.0 交付验收清单

## 基线与当前结论

本清单面向彦祖原始全端 2.0 计划，依据 `docs/海康CVR视频平台开发文档.md` 的功能、并发、安全及交付要求，结合第二版架构、接口约定、当前源码和已有执行记录建立检查矩阵。证据截止本次 2026-09-07 文档核对，不执行测试、服务器操作或部署。后续代码、产物和部署变化必须补充对应版本的证据，不能沿用旧报告自动签收。

当前不能认定全端 2.0 全量验收完成：

- 正式 HTTPS 443 已切换至第二版，当前部署记录为 `2.0.0-20260907093053`；候选和正式入口回滚均有现场执行记录，8443 检查入口保留。证据：`docs/v2-实施进度.md`、`docs/v2-部署运维.md`。
- **第三轮真实容量完整通过，finally 清理完成**。`artifacts/v2-qa/real-capacity/production-800-100-20-verified.json`、同名 `.md` 记录 `status=passed`、`stage=finished`：800 账号、100 并发登录、100 实时共享 1 个 RTSP 上游、20 个独立回放、120 个有效 TS 连接，持续测量 30.281 秒；1500 次普通查询零失败，P95 为 54.386 毫秒，`errors=[]`、`upstreamReleaseVerified=true`，800 个账号均完成停用及角色清理。主代理另确认远程临时 CA 文件已清理，见 R19、R20。**该结果不代表 100 台物理终端解码**。前两轮失败记录仍保留为 R11，不追认或改写。
- **24 小时任务已提前停止，未通过**。运行编号 `20260907T100455Z-965f3d1cc62e`，管理员密码重置撤销测试登录后持续 401，2026-09-08 00:33 停止。最终观察窗口 23271.254 秒包含长时间恢复等待，双路同时有效收流仅约 34 分 33 秒；1767 次失败、2 次断流及 `cleanupComplete=false` 保留。独立核对确认资源已释放、服务正常，自动跟进 ID 24 已暂停，见 R26；后续须使用有效测试凭据及新运行编号。
- **桌面 2.0.1 最终补丁已通过所测实窗流程，正式发布完整通过**。独立 UI 在 `PreferRtsp=true` 下，通道 1 H.264、通道 2 HEVC 主码流自动回退 HTTPS-TS 后画面非空且持续推进；4→16 分屏、选中窗口放大→还原、1920×1080 全屏→Esc 还原全过程两路继续播放，原生控件与底部状态栏不遮挡，见 R22。自动回归 90／90、打包安装 4／4、实际包独立校验 1／1，见 R22、R23。**最终目录为 `artifacts/v2-desktop-2.0.1/`，旧 `artifacts/v2-desktop/` 内未发布 2.0.1 不作最终产物**。R24 发布报告为 `passed`：ZIP ID 4、MSI ID 5，默认 MSI、新旧入口及双包完整下载大小／哈希、Range 206、注销和临时清理通过；`minimumVersion=2.0.0`、`force=false`，双包未签名。全部物理 DPI、16 路真实解码及正式 perMachine（整机）安装仍未验收，当前非管理员进程不能提供整机安装通过结论。

## 状态与证据规则

| 标记 | 使用规则 |
| --- | --- |
| 已实现 | 有具体源码或配置路径，说明交付能力已接入；不表示运行正确或已经验收。 |
| 自动回归通过 | 有已执行日志、TRX 或明确测试记录，注明范围与版本；仅有测试代码不算通过，跳过或空运行不算通过。 |
| 模拟 | 设备、媒体、HTTP 或客户端使用测试替身；即使 API、PostgreSQL 或 FFmpeg 为真实实现，也不升级为录像机实机结论。 |
| 实机通过／抽样通过 | 有真实设备、真实入口、真实安装或现场操作记录，只对该条记录覆盖的范围成立。 |
| 待验收／未通过 | 缺少对应执行证据、仅部分覆盖、运行失败，或修复后尚待复测；不能因源码完整或其他层测试通过而消除。 |

矩阵中的 `R编号` 对应文末证据索引，每项均列出源码入口或执行证据。源码短路径前缀如下：`API/` 为 `src/VideoPlatform.Api/`，`Infra/` 为 `src/VideoPlatform.Infrastructure/`，`Adapter/` 为 `src/Hikvision.Adapter/`，`Desk/` 为 `src/VideoPlatform.Desktop/`，`Web/` 为 `web-admin/src/`。这些前缀只是缩短路径，不是额外的交付模块。

不汇总一个“全部通过数”：78 项集成、26 项管理、44 项 Worker、桌面历史 71／75 项与最终 90 项及安装测试存在不同版本、不同运行和重叠覆盖，不能相加当作完整验收。

## 认证、权限与多设备

| 编号与计划检查项 | 实现依据 | 自动回归与模拟证据 | 实机证据 | 交付判定与待验收 |
| --- | --- | --- | --- | --- |
| A01：Web Cookie／CSRF、桌面 Bearer、并发刷新及退出 | 已实现：`API/Modules/AuthEndpoints.cs`、`Infra/Security.cs`、`Web/stores/auth.ts`、`Desk/Services/SessionService.cs`。 | R01 验证 Web 登录防伪、并发续期不相互撤销；R04、R05 记录客户端认证回归，HTTP 替身与真实 API 分别标注。 | R07：受信任 HTTPS 登录、稳定令牌续期、主动退出通过。 | 自动回归及桌面认证抽样通过；正式 Web 的 Cookie 属性、CSRF 失效和多标签页完整交互仍待逐项实测。 |
| A02：角色与范围同时鉴权、无范围默认拒绝、普通账号不自授管理员 | 已实现：`Infra/Security.cs`、`Infra/AdministrationService.cs`。 | R01、R02：同号通道越权拒绝、多角色合并、范围过滤、最后管理员和并发降权保护通过；设备与媒体端为模拟。 | R06：真实 TS 会话撤销范围后的拒绝及断流抽样。 | 服务端回归通过；不同角色在 Web／WPF 的全部入口、列表与下载需按权限表补验。 |
| A03：停用、改密、单会话撤销，已有媒体／PTZ／导出主动停止 | 已实现：`Infra/AdministrationService.cs`、`Infra/MediaService.cs`、`Infra/Jobs/MediaMaintenanceJob.cs`。 | R02、R03：密码重置、幂等撤销、PTZ 停止、导出取消和有效租约保留；媒体控制为模拟。 | R06：真实 TS 观看者撤权断流，合法共享观看者继续收流。 | 部分实机通过；现有实机撤权检查直连 ZLM HTTP，不能称为完整 HTTPS 撤权链路通过。正式入口下 Web／WPF、回放、下载、PTZ 和通知联合撤权待验。 |
| A04：敏感信息保护、内部密钥、TLS 与审计 | 已实现：`API/Program.cs`、`API/Modules/MediaEndpoints.cs`、`Infra/Security.cs`、`Adapter/Infrastructure/MediaTools.cs`。 | R01：通道响应无设备密码、无内部密钥拒绝；R02：管理写入与权限变更审计；R04 记录媒体诊断脱敏测试。 | R06、R07：HTTPS 正常校验、媒体去令牌拒绝；R10：公开安装包下载校验。 | 对应抽样通过；生产日志脱敏、证书失效／更新以及全部错误路径仍需专门核对，不能以正常登录代替安全验收。 |
| D01：多录像机接入、完整通道同步、不配置摄像机 | 已实现：`API/Modules/DeviceEndpoints.cs`、`Infra/DeviceService.cs`、`Adapter/Devices/DeviceRegistry.cs`。 | R01：两台模拟录像机创建与同步；R03：设备同步并发、一台离线不阻塞其他设备。 | R06、R13：现有 DS-A72124R 的 81 路通道、69 路在线，SDK／ISAPI 基线。 | 单台实机抽样通过；至少两台真实录像机并行接入、独立断线恢复仍待验收。 |
| D02：同号通道全局 ID、媒体复用、报警及 UI 隔离 | 已实现：`database/v2/001_core.sql`、`Infra/MediaService.cs`、`Adapter/Media/LiveSessions.cs`。 | R01 明确验证两台设备相同通道号的不同全局 ID 与越权拒绝；R04、R05、R03 覆盖客户端和设备／报警隔离，含模拟。 | 现有媒体记录仅覆盖一台录像机，见 R06。 | 自动／模拟通过，**真实多设备同号待验收**；需核对树、收藏、轮巡、实时、回放、报警及撤权不串设备。 |
| D03：组织树、通道归属、分页、收藏、稀疏布局与共享轮巡 | 已实现：`Infra/AdministrationService.cs`、`Web/lib/layouts.ts`、`Desk/Services/PlatformApi.cs`。 | R01、R02：组织引用保护、批量移动事务、审计、收藏和共享布局范围过滤；R05、R14：空槽及客户端接入。 | R08 仅有相关页面检查，不能推出全部业务写入流程通过。 | 自动回归有证据；跨端保存恢复、分页大数据、布局空位与轮巡撤权的实机组合待验。 |

## 实时、回放与导出

| 编号与计划检查项 | 实现依据 | 自动回归与模拟证据 | 实机证据 | 交付判定与待验收 |
| --- | --- | --- | --- | --- |
| M01：真实 H.264／H.265 原生主码流与浏览器兼容播放 | 已实现：`Infra/MediaService.cs`、`deploy/nginx-v2.conf`、`Desk/Services/PlayerService.cs`、`Web/lib/media.ts`。 | R04、R05：媒体生命周期与编码处理相关回归；R22：最终桌面 90 项自动回归，不能单独证明设备解码。 | R06：H.264／HEVC HTTPS-TS 抽样解码及 Web 时间推进；R07：两路 WPF 原生截图；R22：最终独立 UI 的通道 1 H.264、2 HEVC 主码流回退 HTTPS 后非空且持续推进。 | 编码与取流抽样通过，最终主窗口已测范围见 U07；多型号、音频及全部录像兼容性仍待验。 |
| M02：1／4／9／16 分屏、拖放、放大、全屏、截图、主子码流切换 | 已实现：`Desk/MainWindow.xaml`、`Desk/Services/PlayerService.cs`、`Web/pages/MonitorPage.vue`。 | R05、R15 保留历史自动证据；R22：最终补丁 90／90 自动回归。 | R21 保留历史分屏黑屏；R22：最终独立 UI 在 4→16、选中窗口放大→还原、1920×1080 全屏→Esc 还原全过程双路主码流继续播放，原生控件与底部状态栏不遮挡。 | **所测分屏、放大还原与全屏流程实机通过**；其他分屏组合、拖放、全部主子码流切换、DPI 组合待补，16 格布局不等于 16 路真实解码。 |
| M03：共享上游、独立播放令牌、续期、幂等停止及断网恢复 | 已实现：`Adapter/Media/LiveSessions.cs`、`Infra/MediaService.cs`、`Infra/Jobs/MediaMaintenanceJob.cs`。 | R01、R03、R04：共享引用、停止重试、过期与续期竞态，设备控制含模拟；R22：最终桌面回退回归。 | R06、R07：合法兄弟流保留和停止释放；R12：两路各续期一次；R19：100 实时共享唯一 RTSP 上游、120 媒体续期及最终释放；R22：双路主码流在 RTSP 不可达时自动回退 HTTPS。 | 对应共享、续期、释放及双路回退实机抽样通过；其他断网自动恢复场景和长期资源稳定仍待验收。 |
| M04：录像检索、缺口、独立／统一回放、暂停／继续／定位／倍速 | 已实现：`API/Modules/MediaEndpoints.cs`、`Adapter/Media/PlaybackSession.cs`、`Desk/Services/PlayerService.cs`。 | R04、R05：片段、缺口、控制及生命周期回归；R16：UTC／时区回归。 | R06：通道 8 固定范围与最近录像的暂停、继续、2 倍速、定位收流和重复停止通过。 | 指定范围实机通过；多通道统一与独立控制、所有倍速、跨日／缺口／定位后旧缓冲及客户端实窗时间轴待验。 |
| M05：回放缓冲有界、溢出明确失败、停止释放和异常恢复 | 已实现：`Adapter/Media/BoundedMediaBuffer.cs`、`Adapter/Media/PlaybackSession.cs`、`Infra/Jobs/MediaMaintenanceJob.cs`。 | R04：字节／条目边界、并发写入、溢出失败；R03：停止失败重试与孤儿核对。 | R06 记录缓冲修复后的控制和收流抽样。 | 自动／模拟及正常流程抽样通过；慢客户端、持续过载和真实 SDK 故障恢复不能视为已验收。 |
| E01：单段 MP4、跨文件 ZIP、原码流封装、UTC 时间与授权下载 | 已实现：`API/Modules/ExportEndpoints.cs`、`Adapter/Exports/ExportService.cs`、`Infra/Jobs/ExportProcessingJob.cs`。 | R03：任务和文件校验；R04：封装边界；R16：真实 PostgreSQL 时区 4 项通过。 | R06：约 122 秒 MP4、含两个 MP4 的跨文件 ZIP、时间清单、抽样解码和授权下载通过。 | 指定录像范围实机通过；更多设备、跨日时区、长任务和完整下载验收需按计划补齐。 |
| E02：导出取消／重试、配额、响应丢失、重启恢复和磁盘不足 | 已实现：`Infra/Jobs/ExportProcessingJob.cs`、`Infra/Jobs/ExportFiles.cs`、`Adapter/Exports/ExportService.cs`。 | R03：并发领取、行锁跳过、响应丢失、取消竞态、旧代结果隔离、配额、权限及重试；R04：磁盘不足、路径与恢复边界。 | R06：运行中取消、重复取消、临时文件清理通过。 | 正常取消实机通过；磁盘不足、服务重启、提交响应丢失和并发配额的实机故障注入仍待验收。 |

## 报警闭环与 PTZ

| 编号与计划检查项 | 实现依据 | 自动回归与模拟证据 | 实机证据 | 交付判定与待验收 |
| --- | --- | --- | --- | --- |
| B01：SDK 回调复制、设备分流、原始／未知报文与图片保留 | 已实现：`Adapter/Alarms/AlarmParser.cs`、`Adapter/Alarms/AlarmJournal.cs`、`Infra/Jobs/AlarmIngestionJob.cs`。 | R04：指针复制、按用户 ID 分流、持久化／重启、损坏行隔离和缓冲溢出健康故障；属于构造报文／模拟。 | R13 仅证明历史实机布防及原始回调进入队列。 | 自动／模拟有记录；**第二版真实报警类型、图片、未知报文逐项触发待验收**，不能将布防成功当作报警全链路通过。 |
| B02：提交后确认、检查点、幂等去重、失败重投与事务发件箱 | 已实现：`Infra/Jobs/AlarmIngestionJob.cs`、`Adapter/Alarms/AlarmJournal.cs`。 | R03 明确覆盖提交先于 ACK、事务回滚无 ACK、幂等重放、空页重新确认、历史／检查点／outbox 一致性；真实数据库、模拟适配器。 | 未见第二版真实报警下进程重启、断网、ACK 丢失和洪峰的完整执行记录，边界见 R03、R13。 | 数据库可靠性回归通过；真实设备重投、报警洪峰、堆积回补与零丢失核对待验。 |
| B03：认领、备注、关闭、重开、操作历史、设备恢复与人工状态分离 | 已实现：`API/Modules/AlarmEndpoints.cs`、`Infra/Jobs/AlarmIngestionJob.cs`、`Web/pages/AlarmsPage.vue`。 | R01：关闭／恢复分离及重开；R03：恢复按设备和通道关联，不误匹配未知通道；R05：客户端处理流程。 | 现有实机媒体报告不包含真实报警人工闭环，见 R06、R13。 | 自动／模拟通过；两个真实客户端同步认领、备注、关闭、重开、图片查看和设备恢复关联待验。 |
| B04：授权 SignalR 推送、断线补取、事件去重与撤权后停止通知 | 已实现：`API/Realtime/EventHub.cs`、`Web/stores/events.ts`、`Desk/Services/EventService.cs`。 | R02 覆盖撤权事务事件；R04、R05 记录客户端逻辑。上述证据不等价于真实多客户端 SignalR 端到端执行。 | 未见同时触发设备报警、客户端断线恢复及撤权的完整实机报告，见 R05、R13 的待验事项。 | **端到端待验收**；验证授权用户收到、无权用户不收到、重连补取及处理历史一致。 |
| P01：方向／速度／缩放、开始／停止与预置位调用 | 已实现：`API/Modules/MediaEndpoints.cs`、`Desk/Services/PtzService.cs`、`Web/lib/ptzLease.ts`。 | R01：开始请求与争抢租约；R04：模拟云台保护；R05：客户端 PTZ 回归。 | R13：历史通道 8 左转开始、1 秒后停止；该记录不是第二版完整 PTZ 验收。 | 基线 SDK 可用，第二版真实方向、速度、缩放、预置位及服务端权限待验；不执行预置位写入配置。 |
| P02：互斥租约、松开／失焦／切换／退出／断网／撤权强制停止 | 已实现：`Infra/MediaService.cs`、`Infra/Jobs/MediaMaintenanceJob.cs`、`Desk/Services/PtzService.cs`、`Web/lib/ptzLease.ts`。 | R01、R02、R03：争抢、撤权和租约超时；R04、R05：客户端迟到响应及停止竞态，设备模拟。 | R13 明确仍有失焦等保护待确认；未见第二版实机完整安全停止矩阵。 | **安全停止实机待验收**，必须观察真实云台运动与停止，HTTP 成功和模拟调用记录不能替代。 |

## Web 与 WPF 界面

| 编号与计划检查项 | 已有实现与证据 | 已执行范围 | 当前判定与剩余验收 |
| --- | --- | --- | --- |
| U01：Web 管理、值守、报警、导出、版本、个人资料及异常状态 | 已实现页面和路由：`Web/router.ts`、`Web/pages/`；R04、R08。 | 41 项逻辑测试记于实施进度；R08 记录部分页面四宽度检查，R06 有两路实际 Web 播放。 | 页面与逻辑已有交付；未记录的页面、弹窗、空／错／加载状态及完整业务闭环仍待验，不把页面存在当作通过。 |
| U02：390 像素 | R08：`artifacts/v2-qa/web-responsive/live-390.png`、`live-controls-390-final.png`、`download-390-final.png`、`accounts-dialog-390.png`。 | 实时、控制区、公开下载与账号弹窗有截图；实施记录记载所查根页面无横向溢出。 | 所列页面检查有证据；其余页面、长文本、触控与完整弹窗操作待补。 |
| U03：768 像素 | R08：`artifacts/v2-qa/web-responsive/accounts-768.png`、`exports-768.png`、`download-768-final.png`。 | 账号、导出与下载页面有截图。 | 所列页面抽样；不能推出全部业务视图均已通过。 |
| U04：1440 像素 | R08：`artifacts/v2-qa/web-responsive/devices-1440.png`、`download-1440-final.png`，另有 `artifacts/v2-qa/web-live-1440.png`。 | 设备、下载及实时页面有截图；`download-1440-partial.png` 是过程证据，不代替最终图。 | 所列页面抽样；补齐报警、回放、复杂弹窗及主要工作流。 |
| U05：1920 像素 | R08：`artifacts/v2-qa/web-responsive/live-1920.png`、`download-1920-final.png`；R06 的视频状态记录。 | 实时宽屏、下载及两路视频尺寸与时间推进有记录。 | 宽屏抽样通过；全量页面与长时间真实播放待验。 |
| U06：WPF 1366×768、1920×1080，各 100%／150%／200% DPI | `Desk/Views/`、`Desk/MainWindow.xaml`；R05 的六组合矩阵及 `screenshots/`；R22 最终实窗抽样。 | 历史 `RenderTargetBitmap` 渲染四视图、六组合，共 24 张图，未改变 Windows 缩放；最新有 1920×1080 全屏实窗证据，未提供完整物理 DPI 矩阵。 | **六组物理屏幕／系统 DPI 组合仍待逐项验收**；不根据截图像素尺寸推断 Windows 缩放，也不将单次全屏扩展为全矩阵通过。 |
| U07：真实主窗口布局、原生视频遮挡、分屏切换／切页／最大化／还原／跨屏 | `Desk/MainWindow.xaml`、`tools/VideoPlatform.Desktop.Tests/MainWindowLayoutTests.cs`；历史 R15、R21，最终 R22。 | 最终自动 90／90；独立实窗验证 4→16、选中窗口放大→还原、1920×1080 全屏→Esc 还原，全过程两路主码流继续播放，原生控件与底部状态栏不遮挡。R21 的容器重建黑屏保留为修复前失败。 | **稳定 Tiles 实例与隐藏保留播放器修复在所测实窗流程通过**；切页、跨屏、其他窗口操作及全部 DPI 组合仍待验，不外推为所有布局／终端通过。RTSP 偏好回退另见 U09。 |
| U08：桌面 2.0.1 修复交付与复验 | `Desk/VideoPlatform.Desktop.csproj`；R22、R23、R24；最终目录 `artifacts/v2-desktop-2.0.1/`。 | 最终独立 UI 所测流程、自动 90 项、打包安装 4＋实际包独立校验 1 项及正式发布通过；ZIP ID 4、MSI ID 5。旧 `artifacts/v2-desktop/` 内未发布 2.0.1 非最终产物。 | **最终补丁所测范围复验、打包与正式发布通过**；全部物理 DPI、16 路真实解码及 perMachine 安装仍待验，双包未签名，不扩大验收范围。 |
| U09：用户 RTSP 偏好不可达时自动回退 HTTPS-TS | `Desk/ViewModels/VideoTileViewModel.cs`、`tools/VideoPlatform.Desktop.Tests/VideoTileFallbackTests.cs`；R22。 | 最终自动 90／90；独立 UI 开启 `PreferRtsp=true`，通道 1 H.264、2 HEVC 主码流自动回退 HTTPS 后画面非空并持续推进。复测后本次新产物 UI 进程正常退出，偏好恢复 false，用户旧窗口未改动。 | **所测双路实时主码流自动回退实机通过**；更早不可达 18555 导致白屏的失败保留为 R20。正常可达 RTSP、回放回退控制及更多断网组合的实机范围仍需独立补验。 |

## 安装、更新与接口交付

| 编号与计划检查项 | 实现依据 | 已执行证据与边界 | 交付判定与待验收 |
| --- | --- | --- | --- |
| I01：MSI／ZIP 双包、内置依赖、产物与发布输入一致 | 已实现：`tools/v2-package.ps1`、`deploy/VideoPlatform.Desktop.wxs`。 | R23：`artifacts/v2-desktop-2.0.1/` 内打包安装 4／4、实际包独立校验 1／1，四项关键文件在 MSI、ZIP、发布目录哈希一致；R24：正式双包完整下载大小及 SHA-256 通过。 | **2.0.1 最终产物与正式下载校验通过**；最终包大小、哈希、发布 ID 及入口见 `docs/v2-桌面2.0.1复测.md`。旧目录内未发布 2.0.1 不作最终产物，R09 保留历史结果。 |
| I02：错误哈希拒绝、失败安装回滚、旧进程重启、正常升级及卸载 | 已实现：`src/VideoPlatform.Updater/Program.cs`、`src/VideoPlatform.Updater/UpdatePolicy.cs`、`Desk/Services/UpdateService.cs`。 | R23：最终 2.0.1 打包安装 4 项包含旧载荷拦截与独立 perUser 安装链路；记录错误哈希拒绝、失败回滚与旧探针重启、正常升级及卸载通过。R09 为历史结果。 | **独立 perUser 安装链路通过**；当前进程非管理员，正式 perMachine 安装、UAC、1.x 升级目录继承及真实整机环境仍待验收。 |
| I03：版本发布／撤回、最低版本、强制更新、公开下载与 Range | 已实现：`API/Modules/ReleaseEndpoints.cs`、`database/v2/003_release_packages.sql`、`tools/v2-publish.py`。 | R24：2.0.1 正式发布 `passed`，ZIP ID 4、MSI ID 5；默认 MSI、新旧 latest、packages 双包及包类型筛选、新旧下载 Range 206、两包完整大小／哈希、注销和临时清理通过。R02、R10 保留历史证据。 | **2.0.1 正式发布与下载子项通过**；`minimumVersion=2.0.0`、`force=false`（报告字段 `forceMsi=false`）。正式撤回／恢复、最低版本实际拦截、强制升级及真实安装联动仍待验，配置值不等于这些场景通过。 |
| I04：代码签名与证书策略 | 已实现 TLS 校验及包完整性检查；R23、R24、`docs/v2-桌面2.0.1复测.md` 与 `docs/v2-部署运维.md`。 | 最终 2.0.1 MSI／ZIP 均未签名；SHA-256 和 HTTPS 不等价于代码签名。 | **未签名为交付限制**；签名证书及其发布流程须单独确认，不能标为签名验收通过。 |
| I05：模块边界、迁移、OpenAPI 与生成客户端接入 | 已实现：`API/ApiContractMetadata.cs`、`src/VideoPlatform.Contracts/`、`tools/VideoPlatform.ClientGenerator/`、`database/v2/`。 | R14：发现 72 个业务操作，缺失元数据补充为 0；R01、R02 使用正式迁移；R05 验证生成客户端接入。 | 实现和对应自动证据齐备；生成结果存在不等于每个生产接口均已实测，新端点／客户端改动需按当前版本重跑契约检查。 |

## 容量、稳定性与部署

| 编号与计划检查项 | 已有证据 | 分层结论 | 剩余验收要求 |
| --- | --- | --- | --- |
| C01：800 账号、100 并发登录、100 实时、20 回放的控制面 | R17：30 秒和 70 秒两轮完整模拟控制接口测试，真实 API／PostgreSQL、模拟适配器与 ZLM；普通查询 P95 约 6.07／5.62 毫秒。 | **仅模拟控制面通过**。不包含真实视频字节、SDK、解码及 HTTPS 成本；早期连接池耗尽失败也保留，不能省略。 | 不将模拟结果写为 100 路真实视频或真实设备完整容量通过。 |
| C02：真实账号、100 并发登录、120 个媒体连接起流 | R19：第三轮 800 账号、100 并发登录、100 实时及 20 独立回放，120 个 TS 连接有效，`status=passed`、`stage=finished`。 | **第三轮真实容量流程通过**；R11 前两轮仅起流子项有效，整体失败记录仍保留。 | 本轮结论限所测设备、账号和媒体配置，不代表所有通道或 100 台物理终端完成解码。 |
| C03：真实唯一 RTSP 上游、完整持续测量、普通查询 P95＜500 毫秒 | R19：100 实时共享 1 个真实 RTSP 上游、20 独立回放；持续测量 30.281 秒，1500 次查询、0 失败，P95 为 54.386 毫秒，`errors=[]`。 | **完整测量与时延门槛通过**，并已核对唯一上游来源；依据第三轮完整报告，不追认前两轮被阻断的测量。 | 保留本轮配置和逐请求证据；结论不扩展为长期稳定性、更多独立设备、硬件解码或数据库故障容量。 |
| C04：真实上游与测试账号清理 | R19：finally 完成、`upstreamReleaseVerified=true`，120 媒体停止、100 登录注销、800 账号停用并清空角色；R20 确认远程临时 CA 文件已清理。R11 第二轮亦有释放通过子项。 | **第三轮清理通过**；本地 JSON 已核对 800 条账号清理均为停用且无角色。 | 后续每轮仍独立核对；不将本次清理推广到其他任务或用 DELETE 成功代替上游状态验证。 |
| C05：多通道带宽、CPU／内存、转码、单桌面 16 路及物理设备上限 | 实现与采样入口：`tools/v2-real-capacity.py`、`tools/v2-soak.py`；R19 为指定配置容量，R22 为最终 UI 双路主码流及 16 格布局抽样。 | **扩大范围待验收**。第三轮容量不等于 100 台独立物理终端解码，16 格中两路继续播放不等于单桌面 16 路真实解码。 | 记录不同通道和码率、CPU／内存／带宽峰值、转码配额、终端解码负载及真实录像机最大回放数。 |
| L01：短时真实连续值守、续期、采样与清理 | R12：`20260907T093532Z-6e4a053a34f8`，90 秒，两路主码流各续期一次，零断流、零恢复，清理完成。 | **90 秒实机试跑通过**，不写为 24 小时通过。 | 为长期运行提供启动路径证据，不把短时测试时长拼接到后续任务。 |
| L02：24 小时真实媒体、资源稳定、故障／恢复完整记录 | 已实现：`tools/v2-soak.py`；R12、R25 保留历史抽样，最终 R26：账号密码重置撤销测试会话后持续 401，00:33 停止，1767 次失败、2 次断流。 | **提前停止，未通过**。观察窗口 23271.254 秒包含恢复等待，实际双路连续收流约 34 分 33 秒；原始清理失败不改写。 | 已核对资源释放并暂停自动跟进 ID 24。工具认证拒绝后终止的修复通过 11 项本地回归；有效测试凭据准备好后以新编号重测，不拼接旧时长。 |
| O01：候选与正式入口切换、公开 URL、证书引用和候选保留 | 已实现：`deploy/v2-install.py`、`deploy/nginx-v2.conf`、`tools/v2-remote-checks.py`；R18。 | **现场切换已执行**；443 为第二版、8443 保留，两入口共用当前实例。 | 后续部署按新版本记录；8443 不是独立备份实例，不能作为服务级高可用证明。 |
| O02：当前指针／配置快照、显式 rollback、原入口和候选恢复 | 已实现：`deploy/v2-install.py`；R18 记录候选及正式入口现场回滚、恢复新版，最终正式切换前快照 `20260907T093443Z-554a3960bba5`。 | **已记录的现场回滚流程通过**。按本次操作前快照恢复，不读取过时 `nginx-v1.conf` 盲目覆盖。 | Nginx 校验失败、健康失败、持久化 pending 后强制中断等自动恢复分支，尚缺全覆盖实机故障注入报告，不能由手动回滚成功推导。 |
| O03：数据库／配置／证书备份及恢复边界 | 已实现：`tools/v2-backup.py`；R18 记录 `/var/backups/video-platform/v2-20260907T092625Z` 等备份。 | **备份有记录，数据库恢复演练待验**。应用回滚不回退数据库迁移。 | 核对备份校验值与受控恢复；验证旧程序兼容迁移，不能把目录存在或入口回滚当作数据恢复成功。 |
| O04：服务健康、日志脱敏、资源回收与长期运维 | 已实现：`Infra/Jobs/`、`tools/v2-remote-checks.py`、`tools/v2-soak.py`；R03、R12、R18。 | Worker 真实数据库回归 44 项通过、设备模拟；有短时资源采样和清理实机记录。 | 当前进程日志、异常恢复、告警洪峰、保留期、磁盘不足与长期趋势需按各自范围补验；不能把单次 health 成功当作运行稳定。 |

## 证据索引

| 编号 | 简短证据路径 | 可支持的结论与限制 |
| --- | --- | --- |
| R01 | `tools/VideoPlatform.IntegrationTests/README.md`；`artifacts/v2-regression/a2fd1231d1b5483abf02c3a2a5608955/integration.log`、同目录 `summary.json`。 | Integration 78 项通过；真实 API／数据库、模拟设备。该轮 Admin 曾失败，不把该轮总体说成全通过。 |
| R02 | `tools/VideoPlatform.Admin.Tests/README.md`；`artifacts/v2-regression/e2d5f2d73c2c4b60ba12ec4e7e723b3b/admin.log`、同目录 `summary.json`。 | 最新管理 26 项通过，包括双包发布；媒体控制模拟，发布测试内容不是正式安装包。 |
| R03 | `tools/VideoPlatform.Worker.Tests/README.md`；`artifacts/worker-tests/worker-postgres.trx`。 | 44 执行、44 通过、0 跳过；真实隔离 PostgreSQL，模拟适配器。故障用例见同目录 `DatabaseTests.cs`、`FailureAndRaceTests.cs`。 |
| R04 | `docs/v2-实施进度.md`；`tools/VideoPlatform.Adapter.Tests/Program.cs`；`web-admin/README.md`。 | 实施记录报告适配器 15 项、Web 41 项通过；测试代码说明覆盖范围，不作为新的执行结果。本清单未取得这两组独立逐轮原始报告。 |
| R05 | `tools/VideoPlatform.Desktop.Tests/验收说明.md`；`tools/VideoPlatform.Desktop.Tests/TestResults/desktop-generated-client-regression.trx`；`tools/VideoPlatform.Desktop.Tests/screenshots/`。 | 历史 71／71 自动回归与 24 张自动渲染图，不代表实际系统 DPI、原生窗口遮挡或实窗交互通过。 |
| R06 | `docs/v2-媒体联调记录.md`。 | 指定录像机、通道、范围的实时／回放／导出／匿名拒绝／TS 撤权记录；直连 ZLM 撤权与 HTTPS 抽样是不同检查。 |
| R07 | `tools/VideoPlatform.Desktop.Tests/candidate-evidence/候选实机验收.txt`；`artifacts/v2-qa/desktop/candidate.trx`。 | 1 项实机测试记录两路原生截图、登录续期及播放器释放；不覆盖真实主窗口布局。 |
| R08 | `docs/v2-实施进度.md`；`artifacts/v2-qa/web-responsive/`。 | 四宽度已有页面截图和根页面溢出检查记录，不外推为全部页面完整验收；具体图见 U02～U05。 |
| R09 | `tools/VideoPlatform.Desktop.Tests/安装包产物一致性.md`；`artifacts/v2-desktop/payload-tests/final-package-payload.trx`；`artifacts/v2-desktop/qa-bc4dec98bb17419a8369617a44c2fba9/安装更新验收.txt`。 | 对应 2.0.0 最终产物一致性与隔离 perUser 安装链路，5 个不同用例；不替代正式 perMachine。 |
| R10 | `artifacts/v2-qa/publish/release-2.0.0-production.json`。 | 正式双包下载与 SHA-256／Range 验证为 `passed`；版本 2.0.0、`forceMsi=false`，不覆盖新版本实窗或强制升级。 |
| R11 | `artifacts/v2-qa/real-capacity/production-800-100-20.md`、同名 `.json`；`artifacts/v2-qa/real-capacity/production-800-100-20-final.md`、同名 `.json`；`tools/v2-real-capacity.md`。 | 两轮总体均失败；120 起流子项有效，第二轮上游释放通过，完整测量未通过。两份记录复用账号命名空间，不合并成一轮成功测量。 |
| R12 | `docs/v2-连续值守.md`；`tools/v2-soak.py`。 | 90 秒试跑与 24 小时启动、首次创建会话 502 的只读原因调查已记录；原检查为历史证据，最新远程停止和核对结果见 R26。 |
| R13 | `docs/设备验证记录.md`。 | 历史 DS-A72124R SDK、通道、布防和基本 PTZ 基线；不能代替第二版报警闭环或安全停止实测。 |
| R14 | `artifacts/v2/generated/metadata-report.md`；`tools/VideoPlatform.ClientGenerator/README.md`；`docs/v2-接口协作契约.md`。 | 72 操作、0 项元数据补充及生成接入约定；不能只凭生成文件宣布全部接口通过。 |
| R15 | `artifacts/desktop-layout-tests/main-window-layout-before.trx`、`main-window-layout-after.trx`、`desktop-layout-regression.trx`。 | 历史依次为 4 失败、4 通过、75 通过；仅自动布局／回归证据。最终补丁的 90 项回归及实际窗口复测另见 R22，不覆盖或删除历史结果。 |
| R16 | `tools/VideoPlatform.Export.Tests/README.md`；`artifacts/export-time-tests/export-time-postgres.trx`。 | 导出时区真实数据库回归 4／4，不代替真实录像文件验证。 |
| R17 | `docs/v2-容量验证.md`。 | 30／70 秒完整模拟控制面结果及原始结果目录；没有真实 SDK 或视频字节容量结论。 |
| R18 | `docs/v2-部署运维.md`；`docs/v2-实施进度.md`。 | 当前 443、部署版本、入口回滚、备份和现场快照记录；描述的所有脚本分支不等于均已故障注入验证。 |
| R19 | `artifacts/v2-qa/real-capacity/production-800-100-20-verified.json`、同名 `.md`。 | 第三轮 `passed`、`finished`，30.281 秒、1500 查询、0 失败、P95 54.386 毫秒、错误数组为空、上游释放及 800 账号清理通过。本清单核对了本地 JSON 和 Markdown，未重新运行。 |
| R20 | 2026-09-07 主代理早期交接；关联路径：R19、`src/VideoPlatform.Desktop/VideoPlatform.Desktop.csproj`、`src/VideoPlatform.Desktop/ViewModels/VideoTileViewModel.cs`、`tools/VideoPlatform.Desktop.Tests/VideoTileFallbackTests.cs`。 | 历史确认临时 CA 远程文件清理、四屏不溢出与 HTTPS 子码流双路可见、原白屏的 RTSP 偏好原因；当时 fallback 修复中。源码仅作定位，现场结论来自交接；最终主码流回退与实窗复测以 R22 为准。 |
| R21 | 2026-09-07 主代理首次长时启动及分屏故障交接；远端路径 `/var/lib/video-platform-v2-soak/runs/20260907T100455Z-965f3d1cc62e/progress.json`、同目录 `events.jsonl`。 | 历史交接为服务 active、两路 streaming，首次 502 后约 9 秒恢复、failureCount=1；当时分屏容器重建黑屏已复现、修复待测，旧 2.0.1 包未发布。保留当时失败事实，后续桌面复测见 R22，最终产物见 R23，502 原因进展见 R25。本轮未访问远端报告。 |
| R22 | `docs/v2-桌面2.0.1复测.md`；`artifacts/desktop-fallback-tests/desktop-fallback-stable-host-regression-current.trx`；`artifacts/v2-qa/desktop-ui/final-2.0.1-rtsp-fallback-main-4.png`、`final-2.0.1-main-16.png`、`final-2.0.1-main-fullscreen-1920.png`。 | 最终自动 90 执行、90 通过、0 失败、0 未执行，本地 TRX 已核对。独立 UI 的双路主码流自动回退、持续推进及切换过程来自主代理实机交接，截图为状态证据，不能单独证明全过程；不覆盖全部物理 DPI 或 16 路真实解码。 |
| R23 | `artifacts/v2-desktop-2.0.1/qa-538785161b8d4849a051ba101f3424ec/安装更新验收.txt`、同目录 `57477_DESKTOP-ANA4LJB_2026-09-07_18_18_11_net10.0.trx`；`artifacts/v2-desktop-2.0.1/payload-tests/final-package-payload.trx`；`artifacts/v2-desktop-2.0.1/payload-test-3b86c181773d40fe83ce75b86dc6529d/产物一致性验收.txt`。 | 本地核对最终打包安装 4／4 与实际包独立校验 1／1，合计 5 项，不与旧轮次相加；安装为独立 perUser，四项关键文件在 MSI、ZIP、发布目录哈希一致。最终目录是 `artifacts/v2-desktop-2.0.1/`，区别于旧目录中的同版本未发布包。 |
| R24 | `artifacts/v2-qa/publish/release-2.0.1-production.json`；包明细见 `docs/v2-桌面2.0.1复测.md`。 | 已核对本地报告：顶层及 remote 均 `passed`，ZIP ID 4、MSI ID 5；默认 MSI、新旧 latest、双包及包类型筛选、新旧 Range 206 和完整双包大小／SHA-256 通过，logout、cleanup 通过，临时文件已清理并保留报告。`minimumVersion=2.0.0`、`forceMsi=false`；主代理确认双包未签名，不表示强制升级或整机安装通过。 |
| R25 | `docs/v2-连续值守.md` 的首次失败调查记录；2026-09-07 历史交接；原运行报告路径见 R21。 | 历史文档记录 RTSP 上游超时、创建会话 POST 502 及恢复时间线，设备瞬时响应或网络原因无法区分。当时交接观察 1246 秒、续期 20／21 次、断流 0、启动 failureCount=1；这些历史值不代表最终结果，后续新增故障与停止见 R26。 |
| R26 | `docs/v2-连续值守.md`；`artifacts/v2-qa/soak/20260907T100455Z-965f3d1cc62e/` 中的原始报告和三份独立核对 JSON；`tools/test_v2_soak.py`。 | 2026-09-08 现场确认账号密码重置、同刻登录撤销及两路断流；已停止工具并归档，最终 stopped、passed=false、1767 失败、2 断流、cleanupComplete=false。独立数据库和媒体核对确认本轮资源已释放，四服务正常，管理员锁定自然到期，跟进 ID 24 已暂停。工具终止认证重试的 11 项本地测试通过，尚未使用新工具做长时实机复测。 |

## 签收检查状态

- [x] 第三轮指定配置的真实容量整轮及清理通过，完整证据为 R19；保留 R11 前两轮失败，不外推为 100 台物理终端解码。
- [ ] 完成新的 24 小时独立验收。旧运行 `20260907T100455Z-965f3d1cc62e` 已提前停止、未通过，最终故障和资源核对见 R26；有效测试凭据准备好后使用新编号，保留旧记录，不拼接时长。
- [x] 最终独立 UI 开启 PreferRtsp，通道 1 H.264、2 HEVC 主码流自动回退 HTTPS 后画面非空且持续推进；最终 90 项自动回归及对应实窗证据见 R22。
- [x] 稳定 Tiles 实例及隐藏保留播放器的最终修复在 4→16、选中窗口放大→还原、1920×1080 全屏→Esc 还原流程通过；全过程两路继续播放，原生控件与底部状态栏不遮挡，见 R22。
- [ ] 补齐其余实窗操作、六组物理 DPI／屏幕组合及 16 路真实解码；R22 的双路播放与 16 格布局不替代这些验收。
- [x] 最终 `artifacts/v2-desktop-2.0.1/` 双包打包安装 4 项、实际包独立校验 1 项通过，见 R23；安装范围为独立 perUser，旧目录的未发布 2.0.1 不作最终产物。
- [x] 2.0.1 正式发布完整通过：ZIP ID 4、MSI ID 5，默认 MSI、新旧入口、双包完整下载大小／哈希与 Range 206、注销和临时清理均通过，见 R24；最低版本 2.0.0、非强制、双包未签名。
- [ ] 完成真实多设备同号、报警端到端闭环／重投／洪峰、PTZ 安全停止及全端联合撤权；当前 R01～R05 的相关设备控制包含模拟。
- [ ] 补齐正式整机安装升级、强制更新、数据库恢复和未覆盖的自动回滚故障分支；当前进程非管理员，perMachine 未验收，范围见 R09、R10、R18、R23。

每个签收条目需附日期、源码或发布版本、设备／终端与 DPI、执行范围、结果、报告路径及残留限制。界面与容量修复后的测试仅更新其覆盖的条目；保留失败记录，不把恢复成功、重连或测试工具修复改写成此前无故障通过。
