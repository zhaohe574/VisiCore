# v0.1.6 发布提案

## 目标

交付接近标准开源项目体验的 Linux Docker 部署包，并把 Release 上传资产限定为可直接安装的包、Windows MSI 与校验清单。

## 不在范围内

不发布 Core Windows 自动安装器，不将 Docker Socket 授予业务容器，不把私钥、恢复密钥、SBOM、签名描述或 staging 证据作为公开 Release 上传文件。
