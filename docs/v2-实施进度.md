# 第二版实施进度

## 当前状态

- [x] 确认需求、服务器基线与现有构建。
- [x] 创建本地源码归档。
- [x] 完成服务器备份及工具链准备。
- [x] 服务端模块、数据库与权限。
- [x] 多设备适配、媒体与导出。
- [x] Web 管理与值守工作区。
- [x] WPF 原生值守及更新器。
- [ ] 完成全部实机验收与正式安装升级；已有部分自动测试、设备联调和隔离安装记录。
- [x] 正式 HTTPS 443 部署、候选与正式入口回滚现场验证。
- [x] 完成服务器真实媒体容量目标验证与报告；不替代百台物理工作站解码和网络验收。
- [ ] 完成 24 小时连续值守验证。

## 验收原则

只记录实际执行结果。编译通过、模拟设备测试、实际设备联调和长时间稳定性验证分别记录，不相互替代。

## 已执行记录

- 源码基线：Git 提交 `37ae68b`；归档 `artifacts/backups/source-20260907-092927.zip`。
- 服务器数据库及部署备份：`/var/backups/video-platform/v1-20260907-012928/`。
- 使用 .NET 10.0.400，API、Worker、适配器及 WPF 均已通过编译。
- 隔离 PostgreSQL 集成验证：78 项平台检查、20 组管理业务检查通过；适配器 14 项测试通过。
- 当前部署位于 `/opt/video-platform-v2/releases/2.0.0-20260907093053`，正式地址为 `https://10.37.200.74`，8443 作为受防火墙限制的检查入口保留，两者共用第二版服务和独立数据库。版本目录末尾时间使用 UTC。
- 实机回放控制、导出、下载、取消以及 H.264/H.265 HTTPS MPEG-TS 抽样解码通过，详见 `v2-媒体联调记录.md`。候选回滚和正式入口回滚均已现场执行并恢复新版；正式入口最终切换前快照为 `20260907T093443Z-554a3960bba5`。
- 最新本地产物：`artifacts/v2/api`、`artifacts/v2/worker`、`artifacts/v2/adapter`、`artifacts/v2-desktop-2.0.1/VideoPlatform.Desktop-2.0.1-x64.msi`、`artifacts/v2-desktop-2.0.1/VideoPlatform.Desktop-2.0.1-win-x64.zip`。平台服务仍为 2.0.0，桌面补丁为 2.0.1。
- Web 在 390、768、1440、1920 像素视口检查账号、设备、导出、视频和公开下载页面，根页面无横向溢出；截图位于 `artifacts/v2-qa/web-responsive`。公开首页使用已有真实值守画面。
- 此前网络套接字限制已经解除，最新候选产物已上传；不能再将网络故障作为当前未完成项的原因。
- 最新适配器自动测试 15 项通过，Web 自动测试 41 项通过；真实隔离 PostgreSQL 导出时区回归 4 项、Worker 回归 44 项通过。历史集成快照成绩不代表后续每次改动均已完整重测。
- WPF 生成客户端接入后自动回归 71 项通过，视觉检查生成 24 张 PNG；正式 HTTPS 地址的两路原生截图、稳定令牌续期和播放器释放实机测试通过。
- MSI／ZIP 双包发布已完成。新增迁移 `003_release_packages.sql` 解除单版本只能保存一个包的限制，管理真实数据库回归增加到 26 项。最终安装与产物一致性测试 5 项通过，新旧下载入口完整文件 SHA-256 与 Range 206 验证通过；报告为 `artifacts/v2-qa/publish/release-2.0.0-production.json`。
- 新增数据库、配置和证书备份：`/var/backups/video-platform/v2-20260907T092625Z`。两路真实主码流 90 秒连续收流、60 秒续期、资源采样与退出清理通过；该短时检查不计作 24 小时验收。
- 正式 HTTPS 容量完整通过：800 个测试账号、100 个并发登录、100 个实时订阅共享 1 个真实 RTSP 上游、20 路独立回放。测量 30.281 秒，1500 次查询零失败，P95 为 54.386 毫秒；120 路 TS 均有效，800 个测试账号已停用解除角色，真实媒体上游和临时文件清理通过。最终报告：`artifacts/v2-qa/real-capacity/production-800-100-20-verified.json`。
- 24 小时运行 `20260907T100455Z-965f3d1cc62e` 已于 2026-09-08 00:33（北京时间）提前停止，未通过。启动 502 后曾恢复，随后管理员密码重置撤销登录，旧工具持续重试未授权请求；最终 1767 次失败、2 次断流，原报告完整保留。
- 桌面 2.0.1 已修复窗口溢出、分屏切换黑屏、RTSP 不可达时回退 HTTPS 及重试预算，90 项自动回归通过。真实 H.264／H.265 主码流在 RTSP 失败后自动恢复；四分屏、十六分屏、放大还原和 1920×1080 全屏的画面持续播放，最终截图位于 `artifacts/v2-qa/desktop-ui/final-2.0.1-*`。这不替代全部物理 DPI 或 16 路同时解码。
- 桌面 2.0.1 最终安装和产物回归 5 项通过，正式 MSI 为 `/api/v2/releases/5/download`，ZIP 为 `/api/v2/releases/4/download`。完整下载哈希、新旧入口及 Range 206 全部通过，报告为 `artifacts/v2-qa/publish/release-2.0.1-production.json`，包均未签名。
- 2026-09-08 已暂停上述运行的自动跟进 ID 24。独立核对确认测试登录全部撤销、媒体和观看连接全部释放、服务正常；工具原始 `cleanupComplete=false` 保留。管理员临时锁定已自然到期。值守工具的认证失败终止修复通过 11 项本地回归，续测需先配置有效测试凭据并使用新运行编号。系统级 MSI 安装、全部物理 DPI、第二版完整真实报警及 PTZ 停止矩阵仍待验收。
